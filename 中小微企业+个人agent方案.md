# 1、知识库

## 1.1 多源数据接入
- **文件类型支持**：PDF、Word、Excel、PPT、Markdown、TXT、图片（OCR）、网页链接、音视频转文字
- **接入方式**：拖拽上传、文件夹监听、API 对接（飞书/钉钉/企业微信文档）、邮件转发自动入库
- **企业系统对接**：支持对接企业已有的 SharePoint、Notion、语雀、Confluence 等

## 1.2 知识加工策略（混合存储）
不是二选一，而是分层存储，各取所长：

| 策略 | 适用场景 | 工具/方式 |
|------|---------|----------|
| **原样保存** | 合同、发票、合规文件等需要原文溯源 | 文件系统 + 全文索引（Elasticsearch/Meilisearch） |
| **RAG 向量化** | 制度文档、FAQ、产品手册等需要语义检索 | Chunk 切割 → Embedding → 向量库（Milvus/Pinecone/Qdrant） |
| **结构化提取** | 表格数据、客户信息、产品参数 | LLM 提取字段 → SQLite/PostgreSQL |
| **知识图谱** | 人员关系、项目依赖、供应商上下游 | Neo4j / 轻量级 JSON Graph |

## 1.3 RAG 最佳实践
- **切割策略**：
  - 按语义段落切割（Markdown 按标题层级，Word 按大纲层级）
  - Chunk size：512~1024 tokens，overlap 10%~15%
  - 表格类内容：不做切割，整表作为一个 Chunk，附带表头描述
- **Embedding 选型**：
  - 中文场景优先：BGE-M3 / text2vec-large-chinese
  - 多语言场景：Cohere Embed v3 / OpenAI text-embedding-3-large
  - 本地部署：BGE-M3（节省成本，数据不出域）
- **检索增强**：
  - 混合检索：向量检索 + BM25 关键词检索（Rerank 后取 Top-K）
  - 多轮检索（Multi-hop）：先检索大纲，再检索细节
  - 查询改写（Query Rewriting）：将用户口语化问题改写为更精准的检索查询

## 1.4 知识保鲜
- **自动更新**：源文件变更时自动触发重新索引
- **过期标记**：时效性内容（如政策、价格）打上时间戳，优先检索最新
- **知识淘汰**：长期未访问 + 已过期的内容自动归档，降低噪音

# 2、性能

## 2.1 模型选型策略
- **任务分流**：
  - 简单任务（摘要、分类、知识检索）→ 轻量模型（DeepSeek-V3 / Qwen-72B）
  - 复杂任务（规划、代码生成、多步推理）→ 强模型（Claude 4 / GPT-4o）
  - 意图识别、路由分发 → 极小模型或规则引擎
- **本地优先**：敏感数据场景推荐本地部署开源模型（Qwen / DeepSeek），数据不出域

## 2.2 响应速度优化
- **流式输出**（Streaming）：首 Token 延迟 < 500ms
- **预加载**：用户输入时预判意图，提前加载相关知识上下文
- **缓存机制**：
  - 语义缓存：相似问题命中缓存直接返回（Redis + 向量相似度）
  - 工具调用结果缓存：同一知识库查询结果 TTL 缓存
- **并发控制**：独立子任务并行执行（知识检索 + 工具调用 可并行）

## 2.3 成本控制
- Token 预算管理：每轮对话设定 Token 上限，超出时触发自动总结压缩
- 分层计费：个人用户低配模型、企业用户可选高性能模型
- 离线批处理：知识库索引、大量文档处理用批处理模式，错峰执行

## 2.4 高可用
- 模型降级：主模型超时/不可用时自动切换备用模型
- 知识库异地备份
- 会话状态持久化，断线重连不丢失上下文

# 3、交互体验

## 3.1 如何做到小白上手，真正智能
- **零配置启动**：
  - 首次使用时，AI 主动引导用户选择角色（开发者/运营/管理者/学生...）
  - 自动扫描用户常用工具和文档，智能推荐初始配置
  - 预设 Skill 模板一键启用，无需手动配置 Prompt
- **意图自动识别**：
  - 用户只需描述"想做什么"，AI自动判断用哪个 Skill / Agent
  - 例如："帮我分析这个季度的销售数据" → 自动激活 xlsx skill + 数据分析流程
- **渐进式引导**：
  - 新手模式：每一步都有 AI 解释"我正在做什么，为什么这么做"
  - 熟练模式：隐藏中间过程，直接出结果
- **容错与自我修复**：
  - 执行失败时自动重试并换策略
  - 出错时用自然语言告知用户原因 + 提供替代方案
- **多模态交互**：
  - 支持语音输入/输出（适合移动办公场景）
  - 截图提问：拍照/截图 → AI 直接分析和操作
  - 文件"扔进来就能用"：PDF/图片/Excel 拖入即分析

## 3.2 如何做到越用越聪明
- **logos 对话总结**：
  - 每次完整任务结束后，logos 自动将对话提炼为结构化笔记
  - 保存到 `your-memory/` 目录（Markdown 格式），支持 Obsidian 双向链接
- **用户偏好学习**：
  - 记录用户偏好的输出格式、代码风格、决策偏好
  - 例如："用户喜欢表格 > 图表"、"代码注释用中文"
- **Skill 自动优化**：
  - 统计 Skill 使用频率和成功率，自动推荐高频组合
  - 用户修改过 AI 输出时，总结修改规律并内化到后续行为中
- **跨会话记忆**（Long-term Memory）：
  - 短期记忆：最近 N 轮对话完整保留（用于当前任务上下文）
  - 长期记忆：关键决策、偏好、项目信息持久化到 SQLite
  - 会话恢复时优先加载用户画像和当前项目上下文

# 4、agent

## 4.1 Knowledge agent（知识整理）

### 数据接入与预处理
- 用户上传文档/数据 → Agent 智能识别文件类型和内容主题
- 自动分类打标签：项目名、日期、数据类型、敏感等级
- 对企业用户：批量导入历史文档，自动归类整理

### RAG 方案选型指南
- 将用户的信息、文档如何放入 RAG 保存：
  - **切割**：Markdown 按标题层级，Word/PDF 按语义段落，Excel 按 Sheet + 行
  - **Embedding**：中文优先 BGE-M3，通过 Ollama 本地部署或调用云 API
  - **存储**：小规模用 ChromaDB/LanceDB（嵌入式），大规模用 Milvus/Qdrant（独立部署）
  - **检索**：混合检索（向量 + BM25），加上 Reranker 精排，Top-5~10 给 LLM

### 记忆整理策略
- **方式一：Obsidian MCP**（推荐个人用户）
  - 结构化笔记存入 Obsidian Vault，自动建立双向链接
  - 支持知识图谱可视化、标签导航
  - 纯 Markdown，完全由用户掌控数据
- **方式二：SQLite**（推荐对检索速度有要求的场景）
  - 存储结构化记忆：用户画像、项目配置、决策历史
  - 支持快速模糊搜索和关联查询
- **方式三：混合方案**（推荐企业用户）
  - 结构化数据 → SQLite
  - 非结构化文档 → RAG 向量库
  - 个人笔记类 → Obsidian / Markdown

### 数据保存策略总结
- **原样保存**：合同、发票、合规文件等高保真要求场景
- **RAG 向量化**：需要语义检索的大量文档
- **结构化提取**：可以从中提取字段的表格化数据
- **三者组合使用**，由 Knowledge Agent 自动判断最佳策略

## 4.2 Agent-team（干活）

### agent-loop 工作流
```
用户需求
    ↓
【Planner】理解需求，拆解任务，输出执行计划
    ↓（用户确认或自动确认）
【Builder】按计划逐步执行：写代码/写文档/做PPT/处理数据
    ↓
【Reviewer】对照原计划 + 用户需求，审查交付物质量
    ↓（不合格则退回 Builder 修改）
最终交付 → Logos 记录本次工作
```

- **Planner（规划者）**：
  - 输入：用户需求 + 近 N 轮对话上下文 + 长期记忆（用户偏好、项目信息）
  - 输出：分步执行计划（含验收标准），等待用户确认或一键执行
  - 能力：需求澄清、任务拆分、资源评估、风险预判
- **Builder（执行者）**：
  - 按计划步骤，调用相应 Skill 和工具
  - 支持的子任务类型：代码编写、文档生成、数据分析、PPT 制作、图片生成等
  - 每完成一步输出阶段性成果，支持用户中途介入调整
- **Reviewer（审查者）**：
  - 对照原始需求和执行计划逐项检查
  - 代码：语法检查、逻辑审查、安全扫描
  - 文档：格式规范、内容准确性、是否偏离需求
  - 不合格时自动给出修改建议，触发 Builder 修复
  - 通过后标记任务完成

### Memory（记忆系统）
- **短期记忆（Session Memory）**：
  - 当前会话完整对话历史
  - 上下文窗口管理：用滑动窗口 + LLM 自动压缩，保留关键信息
  - 压缩策略：每 N 轮对话触发一次总结，只保留决策点和关键信息
- **长期记忆（Persistent Memory）**：
  - 用户画像：角色、偏好、常用 Skill、输出风格
  - 项目上下文：项目结构、技术栈、当前进度、关键决策
  - 知识沉淀：logos 自动整理的笔记
- **存储介质选择**：
  - Markdown 文档 + Obsidian MCP：适合知识可视化、手动翻阅
  - SQLite：适合程序化查询、快速检索
  - 两者共存：SQLite 存结构化索引，Markdown 存可读内容

### Skills 能力矩阵

#### master（技术专家）
- 技术问题搜索全球最佳实践（GitHub、Stack Overflow、官方文档）
- 技术选型、架构设计、性能调优、安全加固
- AI/Agent/LLM/RAG 等前沿技术指导

#### logos（记忆管家）
- 对话自动总结 → 结构化笔记
- 关键决策记录和关联
- 知识库自动编排和索引维护
- 跨会话信息串联

#### sage（思维导师）
- 非技术决策：选型框架、沟通方法论、管理模型
- 问题拆解工具：MECE、第一性原理、金字塔原理
- 学习策略、谈判技巧、项目管理方法

#### 可扩展 Skill 体系
- 预留 Skill 接口，用户/第三方可自定义
- Skill 市场：用户可分享和安装社区 Skill
- 示例扩展 Skill：
  - `xlsx-skill`：Excel 数据分析与可视化
  - `pptx-skill`：自动生成演示文稿
  - `docx-skill`：文档撰写与排版
  - `code-review-skill`：代码审查

## 4.3 AI 助手（交互层）

### 个人用户典型工作流
```
用户："我想做一个个人博客网站"
    ↓
AI 激活 sage/master，和用户讨论：
  - 用什么技术栈？（推荐 Next.js + Vercel 免费部署）
  - 需要哪些功能？（文章、标签、评论、RSS...）
  - 设计风格偏好？
    ↓（多轮讨论，logos 持续记录关键决策）
用户："就按这个方案做吧"
    ↓
Planner 读取对话中的最佳实践 + 用户偏好 → 输出执行计划
    ↓（用户确认）
Builder 逐步执行：
  - 脚手架搭建 → 页面开发 → 样式调整 → 部署配置
    ↓
Reviewer 对照需求审查 → 修复问题 → 确认通过
    ↓
交付 + Logos 记录：项目信息、技术选型、关键决策 → your-memory
    ↓
下次对话时，AI 自动加载项目上下文，无缝续接
```

### 企业用户典型工作流
```
【第一步：知识库构建】
企业上传历史文档 → Knowledge Agent 自动整理：
  - 产品手册 → RAG 向量化
  - 财务报表 → 结构化提取到 SQLite
  - 合同文件 → 原样保存 + 全文索引
  - 会议纪要 → 关键信息提取 + RAG
    ↓
【第二步：日常使用】
员工："根据 Q2 销售数据，帮我写一份给老板的汇报 PPT"
    ↓
AI 自动检索知识库：
  - Q2 销售数据（从 SQLite 结构化数据）
  - 公司 PPT 模板和风格规范（从 RAG）
  - 上次汇报的要点和反馈（从 logos 记忆）
    ↓
Planner → Builder（数据分析 + PPT 生成）→ Reviewer
    ↓
交付符合企业规范的汇报材料
```

### 上下文管理方案
- **完整记忆方案**：保留最近 5~10 轮完整对话（开销可控，信息不丢失）
- **压缩方案**：超出窗口的旧对话用 LLM 自动总结，保留：
  - 关键决策（"用户选择了 React 而不是 Vue"）
  - 约束条件（"预算不超过 5000 元"）
  - 待办事项（"还需要完成单元测试"）
- **分层记忆**：
  - L1：当前任务完整上下文
  - L2：总结后的近期历史
  - L3：logos 持久化的长期记忆