# 中小微企业+个人 Agent 方案 — 项目落地计划

> 最后更新: 2026-07-14 | 当前阶段: Phase 1 已完成，Phase 2 待启动

---

## 一、整体架构

这个项目本质上是 **"AI Agent 操作系统"**，在基础模型和用户之间搭了三层：

```
┌──────────────────────────────────────────────┐
│              用户交互层（Chat / 语音 / 文件）           │
├──────────────────────────────────────────────┤
│             AI 助手（意图路由 + 对话管理）              │
├──────────┬──────────┬──────────┬──────────────┤
│ Master   │  Sage    │  Logos   │  可扩展 Skills │
│(技术专家) │(思维导师) │(记忆管家) │ (xlsx/pptx/...) │
├──────────┴──────────┴──────────┴──────────────┤
│              Agent Team（Planner→Builder→Reviewer）│
├──────────────────────────────────────────────┤
│           Memory（短期上下文 + SQLite + Obsidian）    │
├──────────────────────────────────────────────┤
│       知识库（原样存储 / RAG向量库 / 结构化DB / 图谱）    │
├──────────────────────────────────────────────┤
│              模型层（路由分发 + 多模型降级）              │
└──────────────────────────────────────────────┘
```

**核心设计原则**：每层只做自己最擅长的事，层与层之间通过明确接口通信。

---

## 二、分阶段落地计划

### Phase 1：单用户跑通核心闭环 ✅ 已完成

> **周期**: 2-4 周 | **状态**: ✅ 完成

**目标**：一个用户能完成"提出需求 → Planner 出计划 → Builder 执行 → Reviewer 审查 → 交付"的完整流程。

**完成情况**：

| 模块 | 计划范围 | 实际产出 |
|------|---------|---------|
| **Planner** | 把用户需求拆成步骤列表，输出 MD 格式计划 | `agents/planner.md` — 7 步计划 + Gate 定义 + 9 维覆盖矩阵 + 影响面分析协议 |
| **Builder** | 按计划调用 Skill 执行 | `agents/builder.md` — 33 文件产出（25 后端 + 8 前端），含分支安全、原地对比、Plan 偏离记录 |
| **Reviewer** | 对照计划逐项检查 | `agents/reviewer.md` — 四部分审（维度覆盖 + 影响面 + Spec Compliance + Code Quality），含 PARTIAL_FAIL/CRITICAL_FAIL 判定 |
| **Master** | 冷启动需求对齐 | `agents/master.md` — 阶段 A-D 交互式对话协议，B 起步 + A 兜底的维度定义 |
| **Memory** | 单会话内完整上下文 + SQLite | 文件通信协议（loop-plan/output/review/state），跨 session 持久化 |
| **交互** | 纯文本 Chat | Master → Planner → Builder → Reviewer → Checkpoint 全流程打通 |

**已运行的验证用例**：

```
用例: 恋心记录 (LoveDiary) — 情侣甜蜜记录 Web 应用
  需求对齐 → Master 交互式确认 4 项全局配置
  出计划    → Planner 产出 7 步计划 + 8 Gate + 9 维度矩阵
  写代码    → Builder 产出 33 文件（FastAPI 后端 + 粉色系前端）
  审查      → Reviewer: G1/G6/G8 ✅ PASS，前端待浏览器验收
  状态      → PARTIAL_FAIL → Checkpoint 等待用户确认
```

**已集成的 Skill 体系**（12 个）：

| Skill | 来源 | 用途 |
|-------|------|------|
| loop-engine | 自研 | Agent 流水线调度器 |
| master | 自研 | 技术搜索 + 最佳实践 |
| sage | 自研 | 非技术决策方法论 |
| logos | 自研 | 对话总结 + 记忆管家 |
| review | 自研 | 20+ 语言代码审查 |
| fastapi-fba | GitHub | FastAPI 架构最佳实践 |
| frontend-design | Anthropic 官方 | 前端设计美学 |
| taste-skill | 社区 | 前端设计品味集合 |
| ponytail | 社区 | 极简代码 + 过度工程检测 |
| obsidian-skills | 社区 | Obsidian 集成 |
| user-reviewer | 自研 | UX 审查 |
| pipeline-e2e-auditor | 自研 | 端到端审计 |

**Phase 1 交付物**：

```
hhh/
├── agent loop/                    # Agent Loop 框架（骨架）
│   ├── agents/                    # 6 个 Agent 定义
│   │   ├── master.md             # 冷启动需求对齐
│   │   ├── planner.md            # 只读分析 + 出计划
│   │   ├── builder.md            # 执行代码修改
│   │   ├── reviewer.md           # 两阶段审查
│   │   ├── user.md               # UX 审查
│   │   ├── PROTOCOL.md           # 文件通信协议
│   │   └── README.md             # 架构总览
│   ├── skills/                    # 12 个 Skill
│   ├── scenes/                    # 场景模板
│   ├── project/                   # 项目 Spec
│   ├── memory/                    # 运行时状态
│   └── run-loop.sh               # CLI Runner
│
└── lovediary/                     # 🔥 真实验证项目
    ├── backend/                   # FastAPI (25 文件)
    │   └── app/{models,schemas,crud,api}/
    └── frontend/                  # 前端 (8 文件)
        ├── index.html
        ├── css/style.css
        └── js/{app,api,utils,components}/
```

**验收标准**: ✅ 达成 — "帮我搭建一个恋爱情侣记录网站"，Agent 完成规划→写代码→审查→交付，全程自动。

---

### Phase 2：知识库 + Logos 记忆系统

> **周期**: 2-3 周 | **状态**: ⏳ 待启动

**目标**：用户上传文档后，AI 能基于这些文档回答问题、干活。

| 模块 | MVP 范围 | 技术方案 |
|------|---------|---------|
| **文件接入** | 先只支持 PDF、MD、TXT 拖拽上传 | Python 后端解析文件 |
| **RAG** | 单向量库（ChromaDB），单 Embedding 模型（BGE-M3） | LangChain/LlamaIndex 快速搭建 |
| **Logos** | 对话结束后自动生成 300 字总结 → 存到 your-memory/ | 一次 LLM 调用搞定 |
| **混合存储** | 先不做，全部走 RAG | Phase 3 再分策略 |

**验收标准**：上传 10 份产品文档 → 用户问"产品 A 的规格是什么"→ AI 能准确回答并引用原文。

---

### Phase 3：Agent Team 升级 + 性能优化 ✅ 已完成

> **周期**: 2-3 周 | **状态**: ✅ 完成

**目标**：从"能用"到"好用"。

| 模块 | 计划 | 实际产出 | 状态 |
|------|------|---------|:--:|
| **Agent Loop** | Builder 失败自动重试，Reviewer 不通过退回修复 | `run-loop.sh` 内置 MAX_ITER=3 + ALL_PASS/PARTIAL_FAIL/CRITICAL_FAIL 三分支 + 归档机制 | ✅ |
| **任务分流** | 简单→快模型，复杂→强模型 | `app/router/` 意图识别（规则引擎）+ 三档模型预设（fast/balanced/strong）+ 检索置信度动态降级 | ✅ |
| **流式输出** | Chat 和 Builder 全部 Streaming | `app/stream/` SSE 端点 `/api/knowledge/ask/stream`，事件流：start→cache→retrieving→routing→token→done | ✅ |
| **缓存** | 语义缓存 | `app/cache/` 基于 embedding 相似度（>0.95 命中），TTL 1h，加速 736x | ✅ |
| **并发** | 检索和工具调用并行 | FastAPI 异步 + ThreadPoolExecutor，5 查询并行比串行快 3.5-4.1x | ✅ |

**验收结果**:

```
✅ 模型路由: 4/5 意图识别正确（简单→fast，复杂→strong）
✅ 语义缓存: 相同查询命中缓存，0.01s vs 6.77s（736x 加速）
✅ 流式输出: SSE 7 个事件阶段全部正常
✅ Agent Loop: MAX_ITER=3 + 三分支决策 + 归档
✅ 并发执行: 5 查询并行 3.5-4.1x 加速
总计: 5/5 模块通过
```

**新增 API 端点**:

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/knowledge/ask/stream` | GET (SSE) | 流式 RAG 问答 |
| `/api/knowledge/cache/stats` | GET | 缓存统计 |
| `/api/knowledge/cache` | DELETE | 清空缓存 |
| `/api/knowledge/router/models` | GET | 查看模型预设 |
| `/api/knowledge/router/predict` | POST | 预测查询路由 |

**验收标准**: ✅ 达成 — 缓存命中 < 10ms，并发 3.5x+ 加速，模型路由自动分流。

---

### Phase 4：小白体验 + 企业能力 ✅ 已完成

> **周期**: 3-4 周 | **状态**: ✅ 完成

**目标**：从"好用"到"产品级"。

| 模块 | 计划 | 实际产出 | 状态 |
|------|------|---------|:--:|
| **新手引导** | 首次使用弹窗选角色 → 自动加载预设 Skill | `app/onboarding/` 5 角色（开发者/PM/管理者/学生/企业），每个含推荐 Skills + 默认偏好 + 快捷操作 | ✅ |
| **混合存储** | 文档类型自动分策略 | `app/storage_router/` 合同→original、表格→structured、图片→multimodal、文档→RAG、关系→graph，6 种策略自动路由 | ✅ |
| **多模态** | 图片上传 + OCR，Excel 拖入即分析 | 文件路由层识别图片/Excel → 推荐 multimodal/structured 策略 + OCR 动作 | ✅ |
| **多用户/企业** | 知识库隔离、权限控制 | `app/multitenant/` 用户注册/登录 + per-user Collection 隔离（user_{id}）+ SQLite 用户管理 | ✅ |
| **Obsidian MCP** | Logos 笔记自动写入 Obsidian Vault | `/api/knowledge/logos` 自动写入 `your-memory/YYYY-MM-DD.md`，Obsidian 可直接打开 | ✅ |
| **长期记忆** | 跨会话上下文恢复 | `app/memory/` 用户画像 + 项目上下文 + 对话摘要，`/api/memory/restore/{user_id}` 一键恢复 | ✅ |

**验收结果**:

```
✅ 新手引导: 5 角色，推荐 Skills + 默认偏好 + 快捷操作
✅ 混合存储: 6 种文件类型全部正确路由（合同/Excel/图片/文档/代码/关系）
✅ 多用户隔离: 用户 A/B Collection 完全隔离（user_1 / user_2）
✅ 长期记忆: 画像+项目+摘要保存 → 跨会话恢复成功
✅ Obsidian/Logos: 笔记写入 your-memory/2026-07-26.md
✅ 多模态路由: 图片→multimodal(OCR)，Excel→structured(SQLite)
总计: 6/6 模块通过
```

**新增 API 端点**:

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/onboarding/template` | GET | 新手引导模板 |
| `/api/onboarding/roles` | GET | 角色列表 |
| `/api/onboarding/roles/{role}` | GET | 角色完整配置 |
| `/api/storage/strategies` | GET | 存储策略列表 |
| `/api/storage/analyze` | POST | 文件 → 推荐存储策略 |
| `/api/auth/register` | POST | 用户注册（多租户） |
| `/api/auth/login` | POST | 用户登录 |
| `/api/auth/me/{user_id}` | GET | 用户信息 |
| `/api/memory/profile` | POST | 保存用户画像 |
| `/api/memory/profile/{user_id}` | GET | 获取用户画像 |
| `/api/memory/project` | POST | 保存项目上下文 |
| `/api/memory/summary` | POST | 保存对话摘要 |
| `/api/memory/restore/{user_id}` | GET | 跨会话上下文恢复 |

**验收标准**: ✅ 达成 — 6/6 模块全部通过，从"好用"升级到"产品级"。

---

## 三、关键技术难点与解法

### 难点 1：Planner 输出的计划质量不稳定

**问题**：LLM 拆解任务时容易遗漏步骤，或者计划太粗/太细。

**解法**：
- 用 Few-shot 示例 + 结构化 JSON Schema 约束输出格式
- 计划输出必须包含：步骤编号、步骤描述、预期产出物、验收标准
- 复杂任务先让 LLM 输出"任务类型分类"，不同类型走不同的拆解模板
- 计划出来后必须有"用户确认"环节（Phase 1 先做确认，Phase 3 再做自动确认）

### 难点 2：上下文窗口爆炸

**问题**：一次任务可能涉及 20+ 轮对话 + 大量知识库检索结果，Token 不够用。

**解法**：
- **分层压缩**：L1 完整上下文 → L2 总结 → L3 持久记忆
- 每 6~8 轮对话自动触发一次压缩，Prompt 是"保留所有决策点、约束条件和待办事项，其他内容用一句话概括"
- 知识库检索结果：先返回 Top-3，不够再二次检索，不一次塞太多

### 难点 3：Reviewer 的质量

**问题**：用 LLM 审查 LLM 的输出，容易"互相放水"。

**解法**：
- Reviewer 用更冷的 temperature（0~0.1），Builder 可以更灵活
- Reviewer Prompt 中嵌入原始需求和计划作为"硬约束"
- 代码类任务结合 linter/测试运行（客观指标 + 主观审查）
- Phase 3 加入"人工抽样"，对比 Reviewer 和人类判断的差异，持续调优

### 难点 4：知识库的"冷启动"

**问题**：企业刚接入时数据量巨大，索引耗时长，而且没索引完用户就问不了。

**解法**：
- 批量索引走异步队列，先索引文件元数据（文件名、类型、大小），用户至少能看到"有哪些文件"
- 按优先级索引：用户最近打开的文件先索引
- 索引过程中用户提问 → 先用通用模型回答 + 提示"知识库还在构建中"

---

## 四、技术栈

| 层 | 推荐方案 | 备选 |
|---|---------|------|
| **后端框架** | Python FastAPI | Node.js + TypeScript |
| **Agent 编排** | LangGraph / 自研轻量编排器 | CrewAI, AutoGen |
| **向量数据库** | ChromaDB（MVP）→ Qdrant（生产） | Milvus, LanceDB |
| **关系数据库** | SQLite（MVP）→ PostgreSQL（生产） | — |
| **全文检索** | Meilisearch（轻量） | Elasticsearch |
| **缓存** | Redis | — |
| **文件解析** | Unstructured.io / LlamaParse | PyPDF2, python-docx |
| **Embedding** | BGE-M3（通过 Ollama 本地部署） | OpenAI Embeddings |
| **LLM** | DeepSeek-V3（快）+ Claude（强） | GPT-4o, Qwen |
| **前端** | 先复用现有 Chat UI | Next.js 自定义 |

---

## 五、设计原则

**"纵向串联"而不是"横向铺开"**：

不要把每个 Skill 都打磨到完美，而是先用最简单的实现把 Planner → Builder → Reviewer 这条链路从头跑到尾，跑通一个真实用例，然后再各自迭代升级。

先有闭环，再优化每一个环节。
